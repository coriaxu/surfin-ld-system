# 第 4–20 项功能迭代验收

本清单记录当前修改结果与启用条件。第 19 项的服务器代码已实现并在隔离环境验证，尚未配置正式账号、部署地址或导入个人报告，因此不能视为线上权限已生效。

| 序号 | 修改结果 | 验收 |
|---|---|---|
| 4 | 顶部区分本机已保存、待同步、同步中、同步成功和失败；显示最近同步时间，失败可重试；浏览记录不触发写入 | 同步失败保留副本、冲突停止写入、本机存储失败状态均有测试 |
| 5 | 空白为待回收，旧零值为待核对，明确零值显示 0%；平均率跳过缺失数据 | 广播站新增空白、补录零值实测 |
| 6 | 大屏与月报统一“参训人次”，记录保留人数和时长；人时按场次计算 | 关联培训与月报聚合一致 |
| 7 | 金融列表、看板、项目摘要和月报统一发布口径；补充发布日期、独立回收状态 | 发布未回收、已回收零下载、取消工作流编辑实测 |
| 8 | 广播站前置最新一期、累计/待补录、入口和历史搜索；流程折叠 | 桌面、手机和补录实测 |
| 9 | 记录表单保留关键字段，分类/备注折叠，回收数据可稍后补齐 | 空白可保存，字段验证通过 |
| 10 | 各期独立总人数，明确点击才沿用上一期；新建金融期次也移除旧默认人数 | 沿用按钮、缺失分母和真实零值实测 |
| 11 | 培训记录按月份、项目和关键词筛选；讲师/项目建议；复制既有记录 | 复制后为新记录、搜索结果正确 |
| 12 | 分享月流程折叠，支持材料链接；按关联 ID 更新培训记录 | 修改日期/期数后仍只关联一场培训 |
| 13 | 默认进入开销记录，预算可选，无预算时不生成预实结论 | 无预算仍显示真实开销，比较页解释缺失预算 |
| 14 | 金融默认进入期次与统计，看板作为辅助视图 | 默认导航与列表操作通过 |
| 15 | 总览顺序为本月自动汇总、项目最近更新、近期成果、趋势；年度累计折叠 | 保持自动汇总，不添加登记入口 |
| 16 | 网页内月度总结可选月份，导出为次级操作；导出同步修正人次和预算口径 | 在线摘要与模块数据一致；保留既有导出机制 |
| 17 | 精简案例重复介绍，支持主题和国家关键词搜索 | 国家手册与跨页案例搜索实测 |
| 18 | 前置最新团队报告，按姓名/部门/年份查找已载入报告 | 已载入 2024/2025 报告可搜索；早期年度保留原入口 |
| 19 | 私有服务拦截未登录请求、区分编辑/只读、按个人报告名单授权；同源保存、退出和会话到期 | 隔离账号实测通过；正式账号、服务器及报告迁移待确认，公开站点权限尚未改变 |
| 20 | 四组主导航：工作总览、记录、项目、成果与报告；原模块保留在二级入口 | 桌面导航、手机菜单与表单实测 |

已完成的验证：领域计算、分享月关联更新、三方合并、同步失败与重试、存储失败、服务端身份/权限、公开路径禁止访问、并发版本冲突、跨来源提交拒绝与退出失效。测试只使用隔离的合成记录；没有修改正式业务数据。

补充修复：移除打开分享月看板时自动清空旧格式记录的逻辑；查看工作流改为只生成展示步骤，显式点击步骤后才保存。

发布范围：本轮功能代码与部署说明可随现有站点发布。私有服务需要另外部署；真实账号和个人报告不随公开代码发布。私有环境的操作说明见根目录 README。
